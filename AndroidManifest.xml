<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="androidhive.dashboard"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name="com.androidhive.dashboard.AndroidDashboardDesignActivity" >
            
        </activity>

        <!-- News Feed Activity -->
        <activity android:name="com.androidhive.dashboard.TechnicalEvent" ></activity>

        <!-- Friends Activity -->
        <activity android:name="com.androidhive.dashboard.CulturalEvent" ></activity>

        <!-- Messages Activity -->
        <activity android:name="com.androidhive.dashboard.SportsEvent" ></activity>

        <!-- Places Activity -->
        <activity android:name="com.androidhive.dashboard.FunZoneEvent" ></activity>

        <!-- Events Activity -->
        <activity android:name="com.androidhive.dashboard.AllCollegeEvent" ></activity>
        
        <!-- Photos Activity -->
        <activity android:name="com.androidhive.dashboard.ProfileActivity" ></activity>
        <activity
            android:name="com.androidhive.dashboard.AllProductsActivity"
            android:label="All Products" >
        </activity>

        <!-- Add Product Activity -->
        <activity
            android:name="com.androidhive.dashboard.CreateEvent"
            android:label="Add New Product" >
        </activity>

        <!-- Edit Product Activity -->
        <activity
            android:name="com.androidhive.dashboard.ShowEvent"
            android:label="Edit Product" >
        </activity>
         <activity
            android:name="info.androidhive.login.Login"
            android:label="Login" >
        </activity>
        <activity
            android:name="info.androidhive.splashscreen.SplashScreen"
            android:label="Emblazon" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
         <activity
            android:name="info.androidhive.login.RegisterActivity"
            android:label="Login" >
        </activity>
    
    </application>
 <uses-permission android:name="android.permission.INTERNET" />
    
</manifest>